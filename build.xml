<project name="ABC-Framework" default="build" basedir=".">
    <taskdef name="readSemanticVersion" classname="vendor.setbased.phing-extensions.src.Task.ReadSemanticVersionTask"/>
    <property name="VERSION" value="0.0.0"/>

    <!-- Run composer update and executes various other updates -->
    <target name="update">
        <exec command="composer update" checkreturn="true" passthru="true"/>
    </target>

    <!-- Runs ApiGen -->
    <target name="apigen">
        <exec command="apigen generate --todo --tree --source src --destination doc" passthru="true"
              checkreturn="true"/>
    </target>

    <!-- Creates a new version/release. -->
    <target name="version">
        <readSemanticVersion file=".version" versionProperty="VERSION" haltOnError="true"/>
        <!-- Set version of application -->
        <reflexive>
            <fileset dir=".">
                <include name="src/Application/AbcApplication.php"/>
            </fileset>
            <filterchain>
                <replaceregexp>
                    <regexp pattern="parent::__construct\('ABC', .*"
                            replace="parent::__construct('ABC', '${VERSION}');"/>
                </replaceregexp>
            </filterchain>
        </reflexive>

        <gitcommit repository="." message="Release: ${VERSION}" allFiles="true"/>
        <gitpush repository="."/>
        <gittag repository="." name="${VERSION}"/>
        <gitpush repository="." refspec="${VERSION}" quiet="false"/>
    </target>

    <target name="build">
        <echo message=""/>
    </target>
</project>
